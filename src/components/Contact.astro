---
---
<section id="contact" class="contact">
  <div class="container">
    <div class="contact-wrapper">
      <div class="contact-info">
        <h2 class="section-title" style="text-align: left;">
          İletişime <span class="highlight">Geç</span>
        </h2>
        <p class="contact-desc">
          Bilgisayarınızla ilgili her türlü sorun için bize ulaşın. 
          Ataşehir ve Maltepe bölgesinde en hızlı servis garantisi.
        </p>

        <div class="info-item">
          <div class="icon">📞</div>
          <div>
            <h3>Telefon</h3>
            <p><a href="tel:+905369752873">+90 536 975 28 73</a></p>
          </div>
        </div>

        <div class="info-item">
          <div class="icon">📍</div>
          <div>
            <h3>Adres</h3>
            <p>İçerenköy Mh. Uysal Sk. No:21<br/>Ataşehir, İstanbul</p>
          </div>
        </div>

        <div class="info-item">
          <div class="icon">⏰</div>
          <div>
            <h3>Çalışma Saatleri</h3>
            <p>Pzt - Cmt: 09:00 - 20:00</p>
          </div>
        </div>
      </div>

      <div class="contact-map">
        <!-- Embedded Google Maps Frame Placeholder -->
        <iframe 
          src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3011.6504935478644!2d29.1086!3d40.9768!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zNDDCsDU4JzM2LjUiTiAyOcKwMDYnMzEuMCJF!5e0!3m2!1str!2str!4v1625680000000!5m2!1str!2str" 
          width="100%" 
          height="100%" 
          style="border:0;" 
          allowfullscreen="" 
          loading="lazy">
        </iframe>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    padding: 100px 0;
    background: var(--color-bg-dark);
    /* border-top: 1px solid rgba(255,255,255,0.05); */
  }

  .contact-wrapper {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .contact-info .section-title {
    margin-bottom: 1.5rem;
  }

  .contact-desc {
    color: var(--color-text-muted);
    margin-bottom: 3rem;
    font-size: 1.1rem;
  }

  .info-item {
    display: flex;
    align-items: flex-start;
    gap: 1.5rem;
    margin-bottom: 2rem;
    background: rgba(255, 255, 255, 0.02);
    padding: 1.5rem;
    border-radius: var(--radius-md);
    transition: background 0.3s ease;
  }

  .info-item:hover {
    background: rgba(255, 255, 255, 0.05);
  }

  .icon {
    font-size: 1.5rem;
    background: rgba(0, 255, 157, 0.1);
    color: var(--color-primary);
    width: 50px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
  }

  .info-item h3 {
    margin-bottom: 0.25rem;
    font-size: 1.1rem;
  }

  .info-item p {
    color: var(--color-text-muted);
  }

  .info-item a:hover {
    color: var(--color-primary);
  }

  .contact-map {
    height: 400px;
    border-radius: var(--radius-md);
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .highlight {
    color: var(--color-primary);
  }

  @media (max-width: 768px) {
    .contact-wrapper {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
  }
</style>
